<% if current_user %>
<%= form_with model: @patient do |f|%>
    <table>
        <tr>
            <td>Name</td>
            <td>Comment</td>
        </tr>
        <tr>
            <td><%= f.text_field :name, :value => "Name", :class => "name_field"%></td>
            <td><%= f.text_area :comment, :value => "Comment", :class => "comment_area"%></td>
        </tr>
    </table>
        <%= f.submit "Save"%>
    <%end%>
<% else%>
    <h2> You can not create a patient</h2>
<%end%>